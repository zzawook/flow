import 'package:flutter/material.dart';

/// Styling constants for SpendingScreen.
class SpendingScreenStyles {
  static const backgroundColor     = Color(0xFFF5F5F5);
  static const horizontalPadding   = 18.0;
  static const verticalPadding     = 32.0;
  static const sectionSpacing      = 16.0;
  static const refreshDelay        = Duration(microseconds: 1);
}
