package sg.flow.models.finverse.mappers

import sg.flow.entities.Account
import sg.flow.models.finverse.FinverseAccount

class FinverseAccountToAccountMapper : Mapper<FinverseAccount, Account> {
    override fun map(input: FinverseAccount): Account {
        TODO("Not yet implemented")
    }
}